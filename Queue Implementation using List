class Queue:
    def __init__(self):
        self.q = []

    def enqueue(self, val):
        self.q.append(val)

    def dequeue(self):
        if not self.q:
            return None
        return self.q.pop(0)  # ✅ Remove from front (FIFO)
